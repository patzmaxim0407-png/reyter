import type { Metadata } from 'next';
import HomeView from '@/views/HomeView';

/* ============================================================
   Чернетка каталогу
   ------------------------------------------------------------
   Те саме, що головна, але з неопублікованого каталогу — щоб
   адміністратор побачив магазин очима покупця ще до публікації.

   Окремою адресою, а не параметром ?preview=draft на головній:
   параметр змушував Next рендерити ГОЛОВНУ на кожен запит, разом
   із двома читаннями Firestore. Одна службова сторінка не варта
   того, щоб уповільнювати сайт для всіх.

   Тут кеш недоречний: чернетку дивляться саме тоді, коли щойно
   щось змінили.
   ============================================================ */

export const dynamic = 'force-dynamic';

export const metadata: Metadata = {
  title: 'Чернетка каталогу',
  robots: { index: false, follow: false, nocache: true }
};

export default async function PreviewPage() {
  return <HomeView lang="uk" previewDraft />;
}
